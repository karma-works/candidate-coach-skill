interface:
  display_name: "Candidate Coach Document"
  short_description: "Create evidence-backed modern CVs and application documents"
  default_prompt: "Use $candidate-coach-document to build my application strategy and create a restrained modern HTML/PDF CV and cover letter for this role."
policy:
  allow_implicit_invocation: true
